{
  "name": "medinav-ai",
  "version": "1.0.0",
  "private": true,
  "description": "Safety-first healthcare navigation MVP for hackathon demos.",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "start": "npm run build && node server/server.js",
    "dev": "vite",
    "server": "node --watch server/server.js",
    "test": "node --test server/**/*.test.js",
    "smoke": "node server/smoke.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "mongoose": "^8.19.1",
    "multer": "^2.0.2"
  },
  "devDependencies": {
    "@elevenlabs/client": "^1.25.0",
    "vite": "^8.3.0"
  }
}
